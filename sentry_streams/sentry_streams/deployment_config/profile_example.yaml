
# Default configuration
# As of now, the assumption is that local adapters (like Arroyo)
# do not require overrides of this file
env: {}

pipeline:
  segments:
    - steps_config:
        myinput:
          starts_segment: True
          bootstrap_servers: ["kafka-test-2-001:9092"]
        mybatch:
          batch_size: 1000
          batch_timedelta:
            seconds: 4
        mysink:
          bootstrap_servers: ["kafka-test-2-001:9092"]
