env: {}

pipeline:
  segments:
    - steps_config:
        myinput:
          starts_segment: True
          bootstrap_servers: ["127.0.0.1:9092"]
        parse_arrow:
          batch_size: 1000
          batch_timedelta:
            seconds: 5
        mysink:
          bootstrap_servers: ["127.0.0.1:9092"]
