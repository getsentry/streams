## Summary

Adds [dotagents](https://github.com/getsentry/dotagents) configuration so agents (Claude, Cursor) can use shared skills in this repo.

## Changes

- **agents.toml** – Dotagents config with:
  - Agents: `claude`, `cursor`
  - Trust: `getsentry` GitHub org
  - Skills: `dotagents`, `commit`, `create-branch`, `create-pr`, `iterate-pr` (from getsentry/skills and getsentry/dotagents)
- **.claude/skills** – Symlink to `.agents/skills` for Cursor/Claude skill resolution
- **.gitignore** – Ignore `agents.lock` and `.agents/.gitignore` (dotagents-generated)

## Testing

N/A – config only.
